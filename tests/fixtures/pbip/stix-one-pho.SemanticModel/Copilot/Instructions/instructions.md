# Adventure Works AI Instructions

- Always prefer DISTINCTCOUNT() over COUNT() for customer counts.
- Total Sales lives on the Sales table, not Orders.
- Date filters should use the Date table, never raw OrderDate columns.
