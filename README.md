# fabric-ai-meta

A Python CLI and library that extracts metadata from Microsoft Fabric semantic models, performs AI-driven analysis (table classification, grain detection, measure dependency mapping), and generates AI-ready schemas for external frameworks (LangChain, OpenAI, Semantic Kernel) as well as automated Prep for AI configurations.
